import { Box, Typography } from '@mui/material';

export const UserInfo = () => {
  return (
    <Box>
      <Typography variant="body2">
        name: TEMP
      </Typography>
      <Typography variant="body2">
        login: TEMP
      </Typography>
    </Box>
  );
};
